# @reqly/types

Shared TypeScript types for Reqly monorepo.

## Usage

```typescript
import type { Request, Collection, Environment } from '@reqly/types';
```

## Development

```bash
pnpm build        # Compile TypeScript
pnpm dev          # Watch mode
pnpm typecheck    # Type checking only
```
